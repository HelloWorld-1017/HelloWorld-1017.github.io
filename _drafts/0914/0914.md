# Introduction

之前的两篇博客：[Train an SVM Using Generated Data by MVN-RNG, and Test with Real Data](http://whatastarrynight.com/mathematics/programming/machine%20learning/Train-An-SVM-Using-Generated-Data-by-MVN-RNG-and-Test-Using-Real-Data/)，[Correct Data Leakage Problem](http://whatastarrynight.com/mathematics/programming/machine%20learning/correct-data-leakage-problem/)都考虑使用MVG-RNG来做数据增强。前一篇博客没有考虑数据穿越问题，后一篇做出了纠正。这两篇博客的共同点是都使用生成数据作为所有的训练集数据，考虑的是最坏的情况。但在实践中，数据增强是对数据集的**补充**使得模型可以泛化能力更强，而不是**替代**。因此，如果使用数据增强技术，需要将生成数据和真实数据一起作为训练集训练模型。本文就对比了（1）使用由生成数据构成的完整的训练集（2）使用由生成数据和真实数据混合构成的训练集所训练出的模型的性能。

<br>

# Code

为了使代码更具有通用性，我对整个过程进行了整理，将过程封装成函数，以适应不同问题，并且通过使用MATLAB提供的函数，很大程度上简化了代码的结构。

首先构造了自定义函数`helperConstructTrainAndTestSet.m`：

该函数使用了两个MATLAB提供的两个比较简洁的函数：

- ``，用于partition data；
- ``，用于方便地计算每一种分类的均值和协方差矩阵；

之后，定义了单轮的模型训练和测试函数``：



之后，通过调用`.m`函数，将训练测试的循环过程封装成函数`.m`



该函数将模型多次的训练测试结果打印到命令行中。

最后在代码主体部分，将不同数据集的特征和标签，以及训练测试的轮数、每一类别生成的样本的数量输入到`Loop`函数中：

```matlab
```



# Results

执行的代码主体部分，得到结果：

```matlab
ans =

    '[fisheriris Dataset]
     Model trained with real and generated data
     (1000 Loops, 3120 Train sample numbers): 
     Average of accuracies is 0.9714,
      Standard diviation of accuracies is 0.0285'


ans =

    '[fisheriris Dataset]
     Model trained with ONLY generated data
     (1000 Loops, 3000 Train sample numbers): 
     Average of accuracies is 0.9715,
      Standard diviation of accuracies is 0.0290'


ans =

    '[ionosphere Dataset]
     Model trained with real and generated data
     (1000 Loops, 2281 Train sample numbers): 
     Average of accuracies is 0.8643,
      Standard diviation of accuracies is 0.0372'


ans =

    '[ionosphere Dataset]
     Model trained with ONLY generated data
     (1000 Loops, 2000 Train sample numbers): 
     Average of accuracies is 0.8626,
      Standard diviation of accuracies is 0.0376'


ans =

    '[ovariancancer Dataset]
     Model trained with real and generated data
     (1000 Loops, 2173 Train sample numbers): 
     Average of accuracies is 0.9808,
      Standard diviation of accuracies is 0.0188'
```



两种方法的显著性差异！！！





